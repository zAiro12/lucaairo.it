# matrimonio.lucaairo.it
<VirtualHost *:80>
    ServerName matrimonio.lucaairo.it
    Redirect permanent / https://matrimonio.lucaairo.it/
RewriteEngine on
RewriteCond %{SERVER_NAME} =matrimonio.lucaairo.it
RewriteRule ^ https://%{SERVER_NAME}%{REQUEST_URI} [END,NE,R=permanent]
</VirtualHost>

<VirtualHost *:443>
    ServerAdmin io@lucaairo.it
    ServerName matrimonio.lucaairo.it
    DocumentRoot /var/www/html/matrimonio/

    <Directory /var/www/html/matrimonio/>
        AllowOverride All
        Require all granted
        Options -Indexes +FollowSymLinks
    </Directory>

    ErrorLog ${APACHE_LOG_DIR}/matrimonio-error.log
    CustomLog ${APACHE_LOG_DIR}/matrimonio-access.log combined

    SSLEngine on
    Include /etc/letsencrypt/options-ssl-apache.conf
    SSLCertificateFile /etc/letsencrypt/live/matrimonio.lucaairo.it/fullchain.pem
    SSLCertificateKeyFile /etc/letsencrypt/live/matrimonio.lucaairo.it/privkey.pem

    # CORS
    SetEnvIf Origin "^(https?://(?:.*\.)?lucaairo\.it(:\d+)?|https?://localhost(:\d+)?)$" allowed_origin=$0
    Header always set Access-Control-Allow-Origin "%{allowed_origin}e" env=allowed_origin
    Header always set Access-Control-Allow-Methods "GET, POST, PUT, DELETE, OPTIONS"
    Header always set Access-Control-Allow-Headers "Content-Type, Authorization, X-Requested-With"
    Header always set Access-Control-Allow-Credentials "true"
    Header merge Vary "Origin"
SSLCertificateFile /etc/letsencrypt/live/matrimonio.lucaairo.it/fullchain.pem
SSLCertificateKeyFile /etc/letsencrypt/live/matrimonio.lucaairo.it/privkey.pem
Include /etc/letsencrypt/options-ssl-apache.conf
</VirtualHost>
